TYPE "UDT_Silos" :
STRUCT
    // ===== Дискретні входи (HAL / залізо) =====
    DI_LevelHigh_OK   : BOOL;    // рівень — верхній
    DI_LevelLow_OK     : BOOL;    // рівень — нижній

    // ===== Аналогові входи =====
    AI_Level        : INT;     // аналоговий рівень (raw)
    GrainType       : INT;      //Тип зерна
END_STRUCT;
END_TYPE
